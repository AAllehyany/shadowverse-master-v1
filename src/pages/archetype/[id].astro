---
import { getArchetypeBySlug } from "../../api";
import Layout from "../../layouts/Layout.astro"
const {id} = Astro.params;

const archetypeData = await getArchetypeBySlug(id as string);
---


<Layout title="Archetype Breakdown">
  <div class="w-full max-w-screen-lg mx-auto mt-20 px-5">
    
  </div>
</Layout>